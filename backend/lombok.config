# Stop Lombok from looking for config files higher up the tree.
config.stopBubbling = true

# Mark generated methods with @lombok.Generated so static-analysis tools
# (SpotBugs, JaCoCo) can recognise and skip them.
lombok.addLombokGeneratedAnnotation = true
