# Copy to frontend/.env and adjust. Do NOT commit frontend/.env.

# Base URL the generated API client points at.
# - dev (vite): leave as "/" — vite proxies /api to the backend (see vite.config.ts)
# - prod (nginx): "/" — nginx proxies /api to the backend service
VITE_API_BASE_URL=/
